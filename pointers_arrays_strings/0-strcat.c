#include "main.h"
#include <stdio.h>
#include <string.h>
/**
 * swap_int - check the code
 * @src : char
 * @dest : char
 * Return: dest.
 */

char *_strcat(char *dest, char *src)
{
	int i, *j;
	char strcat;
	while (src[i] != '\0')
	{
		strcat[j] = src[i];
		i++;
		j++;
	}
	i = 0;
	while (dest[i] != '\0')
	{
		strcat[j] = dest[i];
		i++;
		j++;
	}
	strcat[j] = '\0';
	return (strcat)
}