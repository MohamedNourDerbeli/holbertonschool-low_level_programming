#include "main.h"
#include <stdio.h>

/**
 * string_toupper - check the code
 *@* =char
 * Return: Always 0.
 */

char *string_toupper(char *)
{
	int i = 0;

	while (*[i] != '\0')
	{
		if (* >= 97 && * <= 122)
		{
			*[i] = *[i] - 32;
		}
		i++;
	}
	return (*)
}