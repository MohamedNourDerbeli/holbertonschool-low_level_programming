#include "main.h"
/**
 * times_table - Check description
 * Description: It prints 9 times table starting with 0
 * Return: Always 0.
 */

int add(int s, int l)
{
	_putchar((s + l) + '0');
	return (0);
}
