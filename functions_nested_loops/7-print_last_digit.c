#include "main.h"
/**
 * print_last_digit - check the code
 * @r: An integre input
 * Return: void (Success)
 */

int print_last_digit(int r)
{
    int y;
   if (y <0)
   {
    y*= -1;
   }
   _putchar ('0' + y);
   return (y);
}
