#include "3-calc.h"

int main(int argc, char *argv[])
{
	if (argc < 4 || argc > 4)
	{
		printf("Error\n");
		exit(98);
	}
	else
	{
		int a = atoi(argv[1]);
		int b = atoi(argv[3]);
		char s = atoi(argv[2]);

	}
	
}
