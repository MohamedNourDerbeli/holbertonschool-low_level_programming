#include <stdio.h>
/**
 * mul - Check description
 * Description: function that checks for uppercase character.
 * @b: integer
 * @a: integer
 * Return: void (Success) .
 */

int mul(int a, int b)
{
	return (a * b);
}