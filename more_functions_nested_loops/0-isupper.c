#include <stdio.h>
/**
 * _isupper - Check description
 * Description: function that checks for uppercase character.
 * @c: integer
 * Return: void (Success) .
 */

int _isupper(int c)
{
	if (c >= 97)
	{
		return (0);
	}
	else
	{
		return (1);
	}
}
